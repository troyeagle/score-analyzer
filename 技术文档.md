# 曲式分析可视化工具 - 技术文档

## 一、系统架构概述

### 1.1 整体架构
```
┌─────────────────┬─────────────────────────────────────────────┐
│ 应用展示层      │ Vue3 + TypeScript 前端应用                     │
│                 │ ├─ 乐谱渲染面板 (VexFlow 5.0)                 │
│                 │ ├─ 分层标注控制面板 (Element Plus)             │
│                 │ ├─ 排版编辑工具栏                             │
│                 │ └─ PDF导出/渐进演示面板                        │
├─────────────────┼─────────────────────────────────────────────┤
│ 核心业务层      │ 乐谱排版引擎 │ 曲式标注引擎 │ 交互控制引擎     │
├─────────────────┼─────────────────────────────────────────────┤
│ 数据服务层      │ 乐谱解析服务 │ 元数据管理服务 │ PDF导出服务    │
├─────────────────┼─────────────────────────────────────────────┤
│ 数据存储层      │ 本地缓存 │ MusicXML源文件 │ 标注元数据JSON    │
└─────────────────┴─────────────────────────────────────────────┘
```

### 1.2 技术栈详情
- **前端框架**: Vue 3.3+ + TypeScript 5.0+
- **构建工具**: Vite 5.0+
- **乐谱渲染**: VexFlow 5.0+
- **UI组件库**: Element Plus (按需引入)
- **可视化**: SVG + Canvas (分层渲染)
- **PDF导出**: Puppeteer Core + jsPDF
- **状态管理**: Pinia
- **音乐解析**: music21.js + 自定义MusicXML解析器

## 二、模块详细设计

### 2.1 模块1: 乐谱导入与解析模块

#### 功能描述
- 支持标准MusicXML格式文件导入
- 自动解析乐谱基础数据：谱表、小节、音符、调号、拍号、乐句边界
- 读取DCML和声/曲式标注元数据

#### 核心类设计
```typescript
// MusicXML解析结果接口
interface MusicXMLParseResult {
  metadata: ScoreMetadata;
  parts: Part[];
  measures: Measure[];
  notes: Note[];
  annotations: Annotation[];
}

// 乐谱元数据
interface ScoreMetadata {
  title: string;
  composer: string;
  keySignature: string;
  timeSignature: string;
  tempo: number;
}

// 谱表
interface Part {
  id: string;
  name: string;
  measures: string[]; // measure IDs
}

// 小节
interface Measure {
  id: string;
  number: number;
  attributes: MeasureAttributes;
  notes: string[]; // note IDs
  annotations: string[]; // annotation IDs
}

// 音符
interface Note {
  id: string;
  pitch: string;
  duration: number;
  type: string;
  stem: string;
  beam?: string;
}

// 标注
interface Annotation {
  id: string;
  type: 'structural' | 'motivic' | 'harmonic' | 'annotation';
  level: 'basic' | 'advanced' | 'professional';
  startMeasure: number;
  endMeasure: number;
  content: string;
  style: AnnotationStyle;
}
```

#### 核心服务
```typescript
class MusicXMLParser {
  parse(file: File): Promise<MusicXMLParseResult>;
  validate(xml: string): boolean;
  extractMetadata(xml: Document): ScoreMetadata;
  extractParts(xml: Document): Part[];
  extractMeasures(xml: Document): Measure[];
  extractNotes(xml: Document): Note[];
  extractAnnotations(xml: Document): Annotation[];
}
```

### 2.2 模块2: 自定义排版引擎模块

#### 功能描述
- 五线谱行间距自由调节
- 手动拆行/合并行、分页重排
- 保持乐理排版规范
- 页边距、谱面宽度自定义设置

#### 核心类设计
```typescript
interface LayoutConfig {
  pageWidth: number;
  pageHeight: number;
  marginTop: number;
  marginBottom: number;
  marginLeft: number;
  marginRight: number;
  staveSpacing: number;
  systemSpacing: number;
  measurePadding: number;
}

interface StaveLayout {
  id: string;
  x: number;
  y: number;
  width: number;
  height: number;
  measures: MeasureLayout[];
}

interface MeasureLayout {
  id: string;
  x: number;
  y: number;
  width: number;
  height: number;
  notes: NoteLayout[];
}

class LayoutEngine {
  private config: LayoutConfig;
  private staves: StaveLayout[];
  
  constructor(config: LayoutConfig);
  layout(score: MusicXMLParseResult): StaveLayout[];
  updateConfig(config: Partial<LayoutConfig>): void;
  adjustStaveSpacing(staveId: string, spacing: number): void;
  splitMeasure(measureId: string, position: number): void;
  mergeMeasures(measureId1: string, measureId2: string): void;
  repaginate(): void;
}
```

### 2.3 模块3: 曲式分层可视化标注模块

#### 功能描述
- 分层体系：基础结构层、动机层、和声层、注释层
- 图层独立显隐，按难度分级切换
- DCML标准符号标注
- 曲式段落色块区分、动机连线、调性标注

#### 核心类设计
```typescript
interface AnnotationLayer {
  id: string;
  name: string;
  type: 'structural' | 'motivic' | 'harmonic' | 'annotation';
  level: 'basic' | 'advanced' | 'professional';
  visible: boolean;
  opacity: number;
  annotations: Annotation[];
}

interface AnnotationStyle {
  color: string;
  backgroundColor: string;
  borderColor: string;
  borderWidth: number;
  fontSize: number;
  fontFamily: string;
}

class AnnotationEngine {
  private layers: Map<string, AnnotationLayer>;
  private canvas: HTMLCanvasElement;
  private ctx: CanvasRenderingContext2D;
  
  constructor(canvas: HTMLCanvasElement);
  addLayer(layer: AnnotationLayer): void;
  removeLayer(layerId: string): void;
  toggleLayer(layerId: string, visible: boolean): void;
  setLayerOpacity(layerId: string, opacity: number): void;
  renderAnnotation(annotation: Annotation, style: AnnotationStyle): void;
  renderStructuralAnnotation(annotation: Annotation): void;
  renderMotivicAnnotation(annotation: Annotation): void;
  renderHarmonicAnnotation(annotation: Annotation): void;
  clear(): void;
}
```

### 2.4 模块4: 渐进式交互演示模块

#### 功能描述
- 按分析步骤渐进展示
- 单步/自动播放演示
- 乐谱缩放、局部小节聚焦查看

#### 核心类设计
```typescript
interface PresentationStep {
  id: string;
  order: number;
  name: string;
  description: string;
  layerIds: string[];
  duration: number; // ms
}

interface PresentationState {
  currentStep: number;
  isPlaying: boolean;
  speed: number;
  steps: PresentationStep[];
}

class PresentationEngine {
  private state: PresentationState;
  private annotationEngine: AnnotationEngine;
  private layoutEngine: LayoutEngine;
  
  constructor(annotationEngine: AnnotationEngine, layoutEngine: LayoutEngine);
  loadSteps(steps: PresentationStep[]): void;
  nextStep(): void;
  previousStep(): void;
  play(): void;
  pause(): void;
  stop(): void;
  setSpeed(speed: number): void;
  zoomIn(): void;
  zoomOut(): void;
  focusMeasure(measureId: string): void;
}
```

### 2.5 模块5: PDF导出模块

#### 功能描述
- 导出矢量高清PDF
- 支持自定义导出页码、纸张尺寸
- 导出格式适配打印

#### 核心类设计
```typescript
interface PDFExportConfig {
  pageSize: 'A4' | 'A3' | 'Letter';
  orientation: 'portrait' | 'landscape';
  margins: {
    top: number;
    bottom: number;
    left: number;
    right: number;
  };
  includeAnnotations: boolean;
  pageRange?: {
    start: number;
    end: number;
  };
}

class PDFExporter {
  private config: PDFExportConfig;
  
  constructor(config: PDFExportConfig);
  exportToPDF(container: HTMLElement): Promise<Blob>;
  exportToPDFWithPuppeteer(url: string): Promise<Buffer>;
  generatePDFDocument(pages: HTMLElement[]): Promise<Blob>;
}
```

### 2.6 模块6: 元数据管理模块

#### 功能描述
- 本地保存/加载项目文件
- 标注数据手动编辑、修改、保存
- 数据实时同步

#### 核心类设计
```typescript
interface ProjectData {
  id: string;
  name: string;
  createdAt: Date;
  updatedAt: Date;
  musicXML: string;
  layoutConfig: LayoutConfig;
  layers: AnnotationLayer[];
  presentationSteps: PresentationStep[];
}

class ProjectManager {
  private currentProject: ProjectData | null;
  
  createProject(name: string, musicXML: string): ProjectData;
  loadProject(file: File): Promise<ProjectData>;
  saveProject(project: ProjectData): void;
  exportProject(project: ProjectData): Blob;
  updateLayoutConfig(config: Partial<LayoutConfig>): void;
  updateAnnotation(annotation: Annotation): void;
  addAnnotation(annotation: Annotation): void;
  removeAnnotation(annotationId: string): void;
}
```

## 三、接口设计

### 3.1 模块间接口
1. **解析模块 → 排版模块**
   ```typescript
   interface ScoreDataTransfer {
     metadata: ScoreMetadata;
     parts: Part[];
     measures: Measure[];
     notes: Note[];
   }
   ```

2. **标注模块 → 排版模块**
   ```typescript
   interface AnnotationUpdateEvent {
     type: 'add' | 'update' | 'remove';
     annotation: Annotation;
     affectedMeasures: string[];
   }
   ```

3. **导出模块 → 前端界面**
   ```typescript
   interface ExportRequest {
     format: 'pdf' | 'png' | 'svg';
     config: PDFExportConfig;
     containerId: string;
   }
   ```

### 3.2 数据流
```
MusicXML文件 → 解析模块 → 排版模块 → 渲染模块
                                      ↓
                              标注模块 → Canvas图层
                                      ↓
                              交互模块 → 状态管理
                                      ↓
                              导出模块 → PDF文件
```

## 四、开发计划

### 第一阶段：基础架构搭建（第1周）
- [ ] 项目初始化与依赖安装
- [ ] 基础Vue组件架构搭建
- [ ] 核心数据类型定义
- [ ] 基础路由与页面布局

### 第二阶段：核心模块开发（第2-3周）
- [ ] MusicXML解析器实现
- [ ] VexFlow渲染引擎集成
- [ ] 基础排版引擎实现
- [ ] Canvas标注图层基础架构

### 第三阶段：功能完善（第4-5周）
- [ ] 完整标注系统实现
- [ ] 渐进式演示功能
- [ ] PDF导出功能
- [ ] 项目管理与数据持久化

### 第四阶段：测试与优化（第6周）
- [ ] 单元测试编写
- [ ] 集成测试
- [ ] 性能优化
- [ ] 用户反馈收集与调整

## 五、技术难点与解决方案

### 5.1 MusicXML解析复杂性
**问题**: MusicXML格式复杂，包含大量音乐语义信息
**解决方案**: 
- 使用成熟的解析库music21.js
- 分层解析：先提取结构，再解析音乐语义
- 建立中间数据模型，屏蔽格式差异

### 5.2 乐谱渲染性能
**问题**: 大型乐谱（200+小节）渲染卡顿
**解决方案**:
- 虚拟滚动：只渲染可视区域
- 分块渲染：将乐谱分成多个系统分别渲染
- Web Worker：后台处理复杂计算

### 5.3 标注与乐谱位置同步
**问题**: 排版调整后标注位置需要实时更新
**解决方案**:
- 建立小节ID与坐标的映射关系
- 使用响应式数据绑定
- 实现高效的坐标更新算法

### 5.4 PDF导出一致性
**问题**: 浏览器渲染与PDF输出可能存在差异
**解决方案**:
- 使用Puppeteer Core无头浏览器渲染
- 统一渲染引擎，确保一致性
- 矢量图形导出，避免位图缩放问题

## 六、测试策略

### 6.1 单元测试
- 使用Vitest测试框架
- 测试覆盖核心业务逻辑
- 模拟外部依赖

### 6.2 集成测试
- 测试模块间接口
- 测试数据流完整性
- 测试用户交互流程

### 6.3 性能测试
- 大型乐谱加载测试
- 渲染性能测试
- 内存使用监控

### 6.4 兼容性测试
- 主流浏览器兼容性
- 不同屏幕尺寸适配
- 打印输出测试

## 七、部署与维护

### 7.1 开发环境
- 本地开发服务器
- 热更新支持
- 调试工具集成

### 7.2 生产环境
- 静态资源优化
- 代码分割
- CDN部署

### 7.3 监控与日志
- 错误监控
- 性能监控
- 用户行为分析

## 八、风险与应对

### 8.1 技术风险
- VexFlow版本兼容性问题
- MusicXML解析准确性
- PDF导出质量

### 8.2 应对措施
- 建立技术原型验证
- 充分测试边界情况
- 准备备选技术方案

## 九、附录

### 9.1 参考文档
- VexFlow官方文档
- MusicXML规范
- Vue3 + TypeScript最佳实践

### 9.2 术语表
- **MusicXML**: 乐谱交换的XML标准格式
- **VexFlow**: 基于HTML5 Canvas的乐谱渲染引擎
- **DCML**: 数字古典音乐图书馆标注标准
- **曲式分析**: 音乐作品结构分析的方法